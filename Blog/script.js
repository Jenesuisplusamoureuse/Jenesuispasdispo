const openOption = document.getElementById("open-option");
const closeOption = documant.getElementById("close-option");
const openOption = document.getElementById("overlay");

function openOption() {
    overlay.classlist.add("active");
    
}

function openOption() {
    overlay.classlist.remove("active");
    
}

